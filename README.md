# sysfont-dumper

This is a simple homebrew app to dump the current sysfont in bcfnt format to the SD card. Useful for embedding the font in baremetal environments or overriding the font with one from a different region/console.
